<Page x:Class="unoTest.Presentation.NodeLinkTemplatePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:unoTest.Controls"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <utu:NavigationBar Grid.Row="0" Content="{Binding PageTitle}"/>

        <Border Grid.Row="1"
                Margin="16,16,16,12"
                Padding="12"
                CornerRadius="8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Content="初始化"
                            Command="{Binding InitializeCommand}"
                            Style="{StaticResource MaterialOutlinedButtonStyle}"/>
                    <Button Content="儲存到 SQLite"
                            Command="{Binding SaveCommand}"
                            Style="{StaticResource MaterialFilledButtonStyle}"/>
                    <Button Content="重置模板"
                            Command="{Binding ResetTemplateCommand}"
                            Style="{StaticResource MaterialTextButtonStyle}"/>
                </StackPanel>

                <TextBlock Grid.Column="1"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Opacity="0.7"
                           Text="{Binding LastSavedText}"/>
            </Grid>
        </Border>

        <controls:NodeLinkCanvas Grid.Row="2"
                                 Margin="16,0,16,16"
                                 ShowGrid="True"
                                 NodeMinWidth="120"
                                 ViewModel="{Binding Canvas}"/>
    </Grid>
</Page>
