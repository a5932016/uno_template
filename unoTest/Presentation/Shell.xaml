<UserControl x:Class="unoTest.Presentation.Shell"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:unoTest.Presentation"
      xmlns:controls="using:unoTest.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:utu="using:Uno.Toolkit.UI"
      mc:Ignorable="d">

  <!--
    Shell 是應用程式的根容器，包含兩層：
    Row 0：CustomTitleBar — 客製化標題列（跨所有頁面共用）
    Row 1：ExtendedSplashScreen — 頁面導航的內容框架

    DataContext = ShellViewModel
    CustomTitleBar 的 DataContext 明確綁定到 ShellViewModel.TitleBar（TitleBarViewModel）
  -->
  <Grid Background="{ThemeResource BackgroundBrush}">
    <Grid.RowDefinitions>
      <!-- TitleBar 高度固定 -->
      <RowDefinition Height="Auto"/>
      <!-- 頁面內容佔滿剩餘空間 -->
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- ====================================================================
         客製化 TitleBar
         DataContext 明確指向 TitleBarViewModel（ShellViewModel.TitleBar）
         ==================================================================== -->
    <Border x:Name="WindowDragStrip"
            Grid.Row="0"
            VerticalAlignment="Top"
            Height="8"
            Background="Transparent"/>

    <controls:CustomTitleBar x:Name="ShellTitleBar"
                              Grid.Row="0"
                              DataContext="{Binding TitleBar}"/>

    <!-- ====================================================================
         頁面內容區（Uno Extensions Navigation 在此注入頁面）
         ==================================================================== -->
    <utu:ExtendedSplashScreen x:Name="Splash"
              Grid.Row="1"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"
              HorizontalContentAlignment="Stretch"
              VerticalContentAlignment="Stretch">
      <utu:ExtendedSplashScreen.LoadingContentTemplate>
        <DataTemplate>
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="2*" />
              <RowDefinition />
            </Grid.RowDefinitions>
            <ProgressRing IsActive="True"
                  Grid.Row="1"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Center"
                  Height="100"
                  Width="100" />
          </Grid>
        </DataTemplate>
      </utu:ExtendedSplashScreen.LoadingContentTemplate>
    </utu:ExtendedSplashScreen>
  </Grid>
</UserControl>
